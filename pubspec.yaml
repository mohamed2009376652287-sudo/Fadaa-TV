name: fadaa_tv
description: "تطبيق Fadaa TV للبث المباشر"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0

flutter:
  uses-material-design: true
  
